# Phase 14 — Fix prompt (after the audit)

Paste into the **same Claude Code session** that produced `docs/AUDIT-PHASE-14.md` if it is still
open (it has the evidence in context). If it was cleared, start fresh — the prompt names every
file it needs.

---

> Read `docs/AUDIT-PHASE-14.md`, `CLAUDE.md` and `docs/PHASE-PLAN.md`. Phase 14 continues.
> Apply **only** what is approved below, in the order below. Phase 15 does not start here.
>
> ## My decisions on the items that needed a call
>
> **BLOCKER-1 — keep all three. The keep-list is the defect, not the files.** It was written
> without knowing `DESIGN-BRIEF.md`, `DEMO-SCRIPT.md` and `CREDITS.md` existed. Amend the Step 5
> keep-list table in `docs/PHASE-PLAN.md` to include them, one line of justification each: the
> brief holds locked visual decisions no other doc restates · the demo script is what Step 9
> checks the screens against · `CREDITS.md` carries a CC-BY attribution obligation, which is a
> licence term, not documentation. Amend the Phase 14 acceptance bullet the same way. Also keep
> `tools/demo-images/` for the same reason — it is the provenance trail `CREDITS.md` points at.
> Add one sentence under Step 5 saying a keep-list is a *proposal to be checked against the
> code*, never an authority over it.
>
> **HIGH-2 — build "Actually collected".** It is not a new feature; it is a Phase 9 acceptance
> item that was never finished, and a seeded `NoShow` order is reachable in the demo, so the
> panel can walk into the dead end. Scope, exactly: a merchant-only POST action on a `NoShow`
> order → `Completed`, through the same service path an ordinary completion takes — no new
> entity, no new package, no direct `DbContext` write, and the buyer's review must unlock
> exactly as it does after a normal completion. Two tests: the recovery works and unlocks the
> review; a buyer cannot invoke it. **If it turns out to need a schema change, stop and tell me**
> — that would change the decision to "document it as cut".
>
> **LOW-2 — set `TreatWarningsAsErrors` to `true`** for `Faed.Web` in `Directory.Build.props`.
> The tree is already 0-warning, so it costs nothing and stops a plain `dotnet build` from
> hiding a regression. Leave the test project exempt if xUnit analyzers are noisy.
>
> **LOW-3, LOW-4, LOW-5 — no change.** Renaming `CanPlaceB2COrder` touches authorisation for a
> cosmetic gain days before a defence; `COMMITS-AND-COMMENTS.md`'s attribution convention is
> mine to settle separately; `site.css` is adjacent to the `faed.css` rule. Record all three in
> the audit as **accepted, deliberately**, with that reasoning — an accepted finding is not a
> forgotten one.
>
> ## Order of work — one commit per numbered item
>
> Each commit ends with `dotnet build Faed.slnx -warnaserror && dotnet test` green. Do not batch
> unrelated items into one commit.
>
> 1. **Keep-list amendment** (`PHASE-PLAN.md` only).
> 2. **`plan.md` decision-scan — output, do not delete.** Read all 1,701 lines and give me a
>    list of every rule, constraint or reasoning in it that is **not** already stated in
>    `CORE.md`, `BUSINESS-MODEL.md`, `DESIGN-BRIEF.md` or `CLAUDE.md` — quoted, with the doc each
>    belongs in. **Stop there and show me.** Nothing gets deleted until those paragraphs are
>    moved. This is the one deletion where the project has already lost a rule twice.
> 3. **HIGH-1** — the two `_Layout.cshtml` items. Drop the wholesale one, reword the other to
>    "The condition and the reason for the discount stay visible before you reserve." Then grep
>    the whole shared layer — every layout, partial, `<title>`, meta description, footer, error
>    and empty-state view — for the same class of leak before calling it done. A leak in
>    `_Layout` renders on every screen the panel sees, so treat this as a sweep, not two lines.
> 4. **MEDIUM-3** — drop "and controlled brands" from the admin catalogue copy.
> 5. **Deletion commit — D1–D6 plus `Properties/PublishProfiles/` (LOW-1), one commit.**
>    `plan.md` only if step 2 is settled. Fix the two dangling references in the same commit:
>    the `faed.css:8050` comment (approved under rule 3 — a one-line comment change, nothing
>    else in that file) and `PublicMarketplaceModels.cs:167`.
> 6. **HIGH-3 + MEDIUM-4** — regenerate `docs/04-DOMAIN-MODEL.md` from the entities; do not
>    patch the old one. Adopt one definition, **"17 aggregate roots, 21 mapped EF types"**, state
>    it there, and make `CORE.md` §6/§10 and the `PHASE-PLAN.md` result table agree — including
>    CORE §6's bullet list, which currently has 18 names under a title that says seventeen.
> 7. **MEDIUM-1** — rewrite `DEPLOYMENT.md` to the real infrastructure (Brevo `IEmailSender`,
>    `R2FileStorage`, the two hosted services) and **move it to `docs/`** so it matches the
>    keep-list path.
> 8. **MEDIUM-2** — `README.md`: current phase status, the complete documentation table, and a
>    "Demo accounts" block with the seeded accounts and `Demo!Pass123`, marked plainly as
>    Development-only seed data for a public repo.
> 9. **HIGH-2** — "Actually collected" + its two tests, as scoped above.
> 10. **Images** — re-encode the 1.97 MB demo photo and anything else in `wwwroot/` over 500 KB,
>     targeting under 300 KB each at the size actually rendered. The defence machine may be on
>     conference Wi-Fi. Keep the originals in `tools/demo-images/sources/` untouched.
> 11. **MEDIUM-6 + Step 8** — back-fill Phases 11, 12 and 13 into `PHASE-PLAN.md` as completed
>     Do / Acceptance / Verify sections written from what the code does, and record Phase 14's
>     own outcome.
>
> ## Then verify
>
> Re-run **Step 9 in full** — fresh clone, `-warnaserror` build, drop, update, run, seed twice
> for idempotency, unauthenticated smoke pass over every public route and asset. A deleted
> partial, script or image fails at runtime, not at build. Report timings again and diff the
> screens against `docs/DEMO-SCRIPT.md`.
>
> Then give me what I have to run myself, as a new file `docs/REHEARSAL-CHECKLIST.md`: the
> authenticated three-role walkthrough as a numbered checklist — for each step, the account, the
> URL, the control to click, exactly what I should see, and a blank for the elapsed time. Written
> so I can run it on the defence machine without you. Mark the steps `DEMO-SCRIPT.md` depends on,
> and include the `NoShow` recovery from item 9.
>
> Finally update `docs/AUDIT-PHASE-14.md` in place: every finding marked fixed / accepted / cut
> with its commit hash, the deletion list marked applied, the state-of-the-repo table refreshed.
> Do not delete the report — it is the evidence that the audit happened.
>
> Report what you did and stop. If any step reveals something the audit missed, add it to the
> report and tell me rather than deciding it yourself.

---

## After it lands

```bash
git tag phase-14-complete
```

Then run `docs/REHEARSAL-CHECKLIST.md` by hand on the machine the defence will use, and only
then move to the defence package (report · diagrams · slides · backup video · Q&A bank).